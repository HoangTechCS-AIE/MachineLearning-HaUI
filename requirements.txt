# Core ML / data
torch>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Config & utils
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66

# Backend API (phục vụ website)
fastapi>=0.110
uvicorn[standard]>=0.29

# EDA report (tuỳ chọn)
ydata_profiling>=4.6

# Crawl dữ liệu cổ phiếu VN (cần tài khoản FiinQuant).
# Cài bằng:
#   pip install --extra-index-url https://fiinquant.github.io/fiinquantx/simple fiinquantx
# fiinquantx
